{
    "name": "angular-phonecat",
    "private": true,
    "version": "0.0.0",
    "description": "Music album application.",
    "license": "MIT",
    "devDependencies": {
        "bower": "^1.7.7",
        "http-server": "^0.9.0",
        "karma": "^0.13.22",
        "karma-chrome-launcher": "^0.2.3"
    },
    "scripts": {
        "postinstall": "bower install",
        "prestart": "npm install",
        "start": "./node_modules/.bin/http-server ./app -a localhost -p 8000"
    }
}
